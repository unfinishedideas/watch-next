import List from '../classes/List.ts'
import User from '../classes/User.ts'
import Movie from '../classes/Movie.ts'

interface ListViewScreenProps {
        listData: List,
}

const ListViewScreen : React.FC<ListViewScreenProps> = ({listData} : ListViewScreenProps) =>
{
    return(
        <div>
            
        </div>
    )
}
